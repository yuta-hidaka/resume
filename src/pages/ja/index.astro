---
import Layout from '../../layouts/Layout.astro';
import { getData } from '../../data/about.ja';

const data = getData('ja');
const current = data.experience?.[0];
---

<Layout>
  <section class="pt-14 md:pt-20 pb-10 md:pb-14">
    <p class="section-label">Software Engineer — Tokyo</p>
    <h1 class="name-display font-display mt-4">{data.family_name} {data.given_name}</h1>
    <p class="font-display italic text-lg text-[var(--ink-muted)] mt-2" lang="en">Yuta Hidaka</p>
    <p class="text-[var(--ink-secondary)] mt-6">{data.profession}</p>
  </section>

  <section class="glass home-panel grid gap-10 md:grid-cols-[minmax(0,1fr)_200px] md:gap-16 items-start">
    <figure class="portrait md:order-2">
      <img
        src="/me.jpeg"
        alt={`${data.family_name} ${data.given_name}`}
        width="384"
        height="480"
        loading="eager"
      />
    </figure>

    <div class="md:order-1">
      <p class="text-[var(--ink-secondary)] leading-loose">{data.bio}</p>

      <dl class="mt-10 grid grid-cols-[auto_1fr] gap-x-8 gap-y-2 text-sm">
        {current && (
          <>
            <dt class="text-[var(--ink-muted)]">現在</dt>
            <dd>{current.company} — {current.jobTitle}</dd>
          </>
        )}
        <dt class="text-[var(--ink-muted)]">拠点</dt>
        <dd>{data.address}</dd>
      </dl>

      {data.social && (
        <ul class="mt-10 flex flex-wrap gap-x-7 gap-y-2 text-sm">
          {data.social.map((social) => (
            <li>
              <a href={social.url} target="_blank" rel="noopener noreferrer" class="text-link">
                {social.name} ↗
              </a>
            </li>
          ))}
        </ul>
      )}
    </div>
  </section>
</Layout>

<style>
  .name-display {
    font-size: clamp(2.75rem, 9vw, 4.25rem);
    font-weight: 500;
    line-height: 1.15;
    letter-spacing: 0.06em;
  }

  .home-panel {
    padding: 2rem 1.75rem;
    margin: 1.5rem 0;
  }

  @media (min-width: 768px) {
    .home-panel {
      padding: 2.5rem 2.5rem;
    }
  }

  .portrait {
    width: 200px;
    padding: 0.45rem;
    border: 1px solid var(--line);
    border-radius: 18px;
    background: var(--paper-raised);
  }

  .portrait img {
    display: block;
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: 13px;
  }
</style>
